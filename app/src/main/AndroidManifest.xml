<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mang9rme"
        tools:targetApi="31">

        <meta-data android:name="com.kakao.sdk.AppKey" android:value="2f46df15a91f0e2e9507c0a2689ff864"/>

        <activity
            android:name=".kimbsu.feature.enroll.view.EnrollActivity"
            android:exported="false" />
        <activity
            android:name=".kimbsu.feature.intro.view.IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".jihun.presentation.ui.main.view.MainActivity"
            android:exported="true" />
    </application>

</manifest>